# Remove SASS cache files #
app/wp-content/themes/efeqdev/.sass-cache/*

# No need to add the upgrade folder to the repo #
app/wp-content/upgrade/*

# No need to add the uploads folder to the repo #
app/wp-content/uploads/*

# No need to add the plugins folder to the repo #
app/wp-content/plugins/*

# No need to add the config to the repo #
app/wp-config.php

# sitemaps not needed locally #
sitemap.xml
sitemap.xml.gz

# no need for these #
readme.html
license.txt
.log
.DS_Store
